# Historial técnico — Sistema de Inventario DSP

Registro de todo cambio relevante de arquitectura, dependencias o reglas, conforme a la sección 13 del protocolo. Las modificaciones del protocolo indican las secciones afectadas.

| Fecha | Ámbito | Descripción | Motivo |
|---|---|---|---|
| 16-09-2026 | Protocolo, sección 10 | Se deshabilita `Features::registration()` en `config/fortify.php` y se eliminan la vista `pages::auth.register` y su declaración `Fortify::registerView()`. | El registro público estaba habilitado contra lo declarado en la sección 10. Las cuentas se crean desde el mantenedor de usuarios. |
| 16-09-2026 | Protocolo, sección 10 | Se incorporan las passkeys al protocolo y la tabla `passkeys` al MER. | La funcionalidad venía habilitada desde el starter kit y no estaba documentada. Se decide mantenerla por ser un segundo mecanismo de acceso ya operativo, sin dependencias adicionales. |
| 16-09-2026 | Protocolo, secciones 2 y 13 | Se versiona `/.ai/guidelines/` en el repositorio: `.gitignore` pasa de `/.ai` a `/.ai/*` más la excepción `!/.ai/guidelines/`. | El protocolo y su historial técnico quedaban fuera del control de versiones, contra la sección 2. `CLAUDE.md` permanece excluido por ser un archivo generado con `boost:update`. |
| 16-09-2026 | Protocolo, sección 13 | El historial técnico se traslada desde `.ai/guidelines/protocolo-base.md` a `reportes/historial-tecnico.md`. | El protocolo define reglas; el registro de cambios es un documento aparte del proyecto. |
| 16-09-2026 | Base de datos, tabla `users` | Se agrega la columna `activo` (boolean, default true, indexada) directamente en `0001_01_01_000000_create_users_table.php` y se incorpora al MER. | Decisión de la Fase 2: las cuentas se desactivan en lugar de eliminarse, porque `procesos_verificacion.user_id` y `verificaciones.user_id` son `restrictOnDelete` y el historial debe conservarse. |
| 16-09-2026 | Protocolo, sección 4.5 | Se prohíbe crear migraciones que modifiquen tablas ya creadas por otra migración del proyecto. Los cambios se hacen en la migración de creación y se reconstruye con `migrate:fresh --seed`. | Instrucción del usuario, para mantener una sola definición por tabla. |
| 16-09-2026 | `RolesYPermisosSeeder` | Se agrega `forgetCachedPermissions()` después de crear el catálogo de permisos y antes de asignarlos a los roles. | `migrate:fresh --seed` fallaba con `PermissionDoesNotExist: usuarios.ver`, porque la caché de Spatie quedaba obsoleta entre la creación y la asignación. |
| 16-09-2026 | Autenticación | Las cuentas con `activo` en falso no pueden ingresar: `Fortify::authenticateUsing()` bloquea el acceso con credenciales y el middleware `AsegurarCuentaActiva`, agregado al grupo `web`, cierra la sesión en cualquier otra vía. | El ingreso con passkey y las sesiones ya abiertas no pasan por la validación de credenciales de Fortify. El mensaje de login se mantiene genérico para no revelar qué correos existen. |
| 16-09-2026 | Navegación | Se define el reparto de los módulos: cinco secciones temáticas en el `flux:navbar` y sus subsecciones en el `flux:navlist`. | Pendiente declarado en `decision-layout-navegacion.md`, que condicionaba la Fase 3. |
| 16-09-2026 | Rutas | La ruta raíz pasa a ser `inicio`, un componente Livewire, y se retira la ruta `dashboard` y la ruta pública `home`. `fortify.home` apunta a `/`. | Punto 3 de la Fase 3 y sección 6 del protocolo: rutas en español declaradas con `Route::livewire()`. Los layouts de autenticación pasan a enlazar `login`, porque `/` ahora exige sesión. |
| 16-09-2026 | Layout | Se adopta el patrón de encabezado con navegación secundaria: `flux:navbar` en el header, `flux:navlist` en una columna de 220px y `flux:sidebar` solo en móvil. | Decisión registrada en `decision-layout-navegacion.md`, punto 1 de la Fase 3. |
| 16-09-2026 | Layout | Se retira `class="dark"` del elemento `<html>` en los cinco layouts. | La apariencia queda gobernada por `@fluxAppearance`, que ya estaba en `partials/head.blade.php`. |
| 16-09-2026 | Navegación | La estructura del menú vive en `config/navegacion.php` y la resuelve `NavegacionService`, que filtra por permiso y por existencia de la ruta. | Permite declarar de una vez los 13 módulos y que cada entrada aparezca sola a medida que se construyen sus rutas. |
| 16-09-2026 | Módulo Tipos | Se construye el catálogo de tipos: `TipoDTO`, `GuardarTipoDTO`, `TipoRepository`, `TipoService`, componente `pages::tipos.indice` y ruta `/tipos` con `permission:tipos.ver`. | Módulo 4 del orden de desarrollo, puntos 1, 4 y 5 de la Fase 4. La eliminación se bloquea cuando el tipo tiene ítems asociados, porque `items.tipo_id` es `restrictOnDelete`. |
| 16-09-2026 | Módulo Ubicaciones | Se construye el catálogo de ubicaciones: DTOs, `UbicacionRepository`, `UbicacionService`, componente `pages::ubicaciones.indice` y ruta `/ubicaciones` con `permission:ubicaciones.ver`. | Módulo 5 del orden de desarrollo, punto 2 de la Fase 4. La eliminación se bloquea con ítems asociados y también con verificaciones registradas, para no perder la ubicación observada en una toma de inventario. |
| 16-09-2026 | Fase 0, punto 1 | `funcionarios.rut` pasa a `nullable`, conservando el índice único. Cuando se informa, se valida formato y dígito verificador por módulo 11, y se normaliza a la forma `12345678-9`. | Decisión del usuario. Los nulos no colisionan en un índice único, lo que permite registrar responsables sin RUT sin inventar datos. |
| 16-09-2026 | Fase 0, punto 2 | Los responsables que no son personas naturales, como los centralistas de turno, se registran como funcionarios normales, sin RUT. | Decisión del usuario: "ese llamado centralista debe ser ingresado como cualquier otro funcionario". Es como opera hoy la DSP, que asigna 68 bienes a `Centralista 1`. |
| 16-09-2026 | Módulo Funcionarios | Se construye el catálogo: DTOs, `FuncionarioRepository`, `FuncionarioService`, componente `pages::funcionarios.indice` y ruta `/funcionarios` con `permission:funcionarios.ver`. | Módulo 6 del orden de desarrollo, punto 3 de la Fase 4. No hay eliminación: el catálogo solo activa y desactiva, para conservar el historial. |
| 16-09-2026 | Fase 0, punto 3 | La jerarquía `item_padre_id` se usa desde el primer registro, disponible al crear y al editar. El Service impide la autorreferencia y las referencias circulares en cualquier nivel. | Decisión del usuario. La planilla respalda el caso con 8 kits de radio, 8 baterías y 8 monófonos sin vínculo entre sí. |
| 16-09-2026 | Módulo Ítems | Capa de datos del registro maestro: `ItemDTO`, `GuardarItemDTO`, `ItemRepository` e `ItemService`, con 25 pruebas. | Fase 5, puntos 1 a 5, 7 y 8. La eliminación lógica se implementa escribiendo `deleted_at` y filtrando esa columna en cada consulta, porque sin Eloquent no existe `SoftDeletes`. |
| 16-09-2026 | Módulo Ítems | Se construye la presentación: componente `pages::items.indice` con listado, siete filtros, formulario de trece campos, eliminación lógica y alerta de vencimientos, y ruta `/items` con `permission:items.ver`. | Fase 5, puntos 1, 2, 3, 5, 7 y 8. |
| 16-09-2026 | Módulo Ítems | Se construye la ficha del bien en `pages::items.ficha`, ruta `/items/{item}`, con datos, componentes, historial de verificaciones e historial de cambios. | Fase 5, punto 6. El historial de verificaciones se alimenta de `ItemRepository::verificacionesDe()` y queda vacío hasta que exista el módulo de la Fase 7. |
| 16-09-2026 | Fase 3, punto 2 | Se cablea el placeholder: `config/livewire.php` pasa a `'component_placeholder' => 'partials.placeholder'`. | El archivo existía desde la Fase 3 sin uso. Solo actúa sobre componentes marcados con `#[Lazy]`, decisión aún pendiente. |
| 16-09-2026 | Fase 3, punto 6 | Se extraen cuatro componentes compartidos en `resources/views/components/mantenedor/`: `encabezado`, `filtros`, `confirmar-eliminacion` y `acciones`, aplicados a los cinco mantenedores. | Los listados de tipos, ubicaciones, funcionarios, usuarios e ítems repetían la misma estructura. |
| 16-09-2026 | Corrección | `passkey-verify` redirigía a `/dashboard`, ruta eliminada en la Fase 3. Pasa a `route('inicio')`. | El ingreso con passkey terminaba en un 404. Ninguna prueba lo cubría porque el flujo ocurre en JavaScript. |
| 16-09-2026 | Fase 3, punto 5 | Se normalizan 36 clases `stone`, `gray` y `neutral` a `zinc` en el modal de 2FA y dos layouts de autenticación, y se elimina `dashboard.blade.php`, huérfano tras el cambio de rutas. | Sección 5.6 del protocolo. |
| 16-09-2026 | Protocolo, sección 5.6 | Se registra la excepción de contraste para `text-black` bajo `dark:` y se explicita la prohibición de las escalas `gray`, `slate`, `neutral` y `stone`. | Decisión del usuario. Cambiar esos cinco casos dejaría el texto ilegible sobre fondo claro invertido. |
| 16-09-2026 | Fase 0, punto 5 | Se autorizan e instalan `maatwebsite/excel` 4.0.3 y `barryvdh/laravel-dompdf` 3.1.2. | Decisión del usuario. Desbloquea la exportación a Excel y PDF de la Fase 9. Ninguno es paquete de interfaz, por lo que no colisiona con la restricción de Flux UI Pro. |
| 16-09-2026 | Fase 1, punto 7 | Se agregan `TiposSeeder` con 60 tipos y `UbicacionesSeeder` con 10 ubicaciones, tomados de la planilla y registrados en `DatabaseSeeder`. | Los nombres se cargan tal cual, salvo la unificación de `Antena` y `antena`, que colisionaban con el índice único. Queda fuera `Fuera de la dependencia`, que marca un estado y no un lugar, y se agrega `Vía pública` para las cámaras instaladas en la ciudad. |
| 16-09-2026 | Carga diferida | Los cinco listados llevan `#[Lazy]` y usan el placeholder de `partials.placeholder`. | Decisión del usuario. Obligó a adaptar seis pruebas: con carga diferida la primera respuesta trae el esqueleto, y `Livewire::test()` también, por lo que las aserciones sobre contenido usan `Livewire::withoutLazyLoading()`. Cuatro `assertDontSee` pasaban de forma trivial y habrían ocultado un fallo de permisos. |
